sudo: false
dist: trusty
language: node_js

env: 
  global: 
    - MAIN=main 
  jobs:
    include:
      - stage: unit tests
        script: echo one
      - stage: unit tests
        script: echo two
